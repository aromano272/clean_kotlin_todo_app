<resources>

    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="MySearchViewStyle" parent="Widget.AppCompat.Light.SearchView">
        <item name="colorControlNormal">@color/white50</item>
        <item name="colorControlActivated">@color/white70</item>
    </style>

    <style name="MyToolbar" parent="@style/ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="android:background">@color/colorPrimary</item>

        <item name="android:textColorPrimary">@android:color/white</item>

        <item name="android:textColorPrimaryInverse">@android:color/white</item>
        <item name="android:textColorSecondary">@android:color/white</item>

        <!--<item name="drawerArrowStyle">@style/WhiteDrawerIconStyle</item>-->
        <item name="android:itemTextAppearance">@style/menuItemColor</item>

        <item name="colorAccent">@color/white</item>
    </style>

    <style name="menuItemColor">
        <item name="android:textColor">@android:color/black</item>
    </style>


</resources>
